export SEARCH_HOME=$4

if [ ${4:0:1} != "/" ]
then
  export SEARCH_HOME=$HOME/$SEARCH_HOME
fi

cd $SEARCH_HOME

mkdir -p $SEARCH_HOME/logs

nohup $SEARCH_HOME/bin/runclass com.sonicbase.cli.Deploy $1 $2 > $SEARCH_HOME/logs/deploy.log 2>&1 &

